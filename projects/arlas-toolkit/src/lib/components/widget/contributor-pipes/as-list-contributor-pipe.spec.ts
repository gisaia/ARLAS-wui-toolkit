import { AsListContributorPipe } from './as-list-contributor-pipe';

describe('AsListContributorPipe', () => {
  it('create an instance', () => {
    const pipe = new AsListContributorPipe();
    expect(pipe).toBeTruthy();
  });
});
